import NavItems from "./navItems";

const Nav = () => {
  return (
    <ul className="flex flex-row gap-3 pt-3 text-sm text-text-color ">
      <NavItems />
    </ul>
  );
};

export default Nav;
