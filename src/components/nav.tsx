import TabItems from "./tabItems";

const Nav = () => {
  return (
    <ul className="flex flex-row gap-3 pt-3 text-sm text-text-color ">
      <TabItems />
    </ul>
  );
};

export default Nav;
